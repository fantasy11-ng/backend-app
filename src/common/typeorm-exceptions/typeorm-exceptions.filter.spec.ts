import { TypeormExceptionsFilter } from './typeorm-exceptions.filter';

describe('TypeormExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new TypeormExceptionsFilter()).toBeDefined();
  });
});
